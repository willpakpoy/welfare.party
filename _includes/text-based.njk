{% extends "base.njk" %}

{% block content %}
    <div class="header-block-outer">
        <div class="header-block-inner">
            <h1>{{title}}</h1>
            <p>{{subtitle}}</p>
        </div>
    </div>
    <div class="paragraph">
        {{content | safe}}
    </div>
{% endblock %}